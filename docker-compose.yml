version: "3.9"
services:
  sentinel-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - chroma-data:/app/.chroma
    restart: unless-stopped
volumes:
  chroma-data:
